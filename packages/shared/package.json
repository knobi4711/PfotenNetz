{
  "name": "@pfotennetz/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "clean": "rm -rf dist node_modules/.cache"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
